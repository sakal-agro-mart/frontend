import React from "react";
import { RouteProps } from "react-router-dom";

const Categories = (props: RouteProps) => {
  return <div>THis is the categories page lmao</div>;
};

export default Categories;
